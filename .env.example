# MongoDB must run as a replica set (transactions). docker-compose.yml provides one.
MONGO_URI=mongodb://127.0.0.1:27017/procyon?replicaSet=rs0&directConnection=true
REDIS_URL=redis://127.0.0.1:6379
PORT=3000

# Webhook replay protection
TIMESTAMP_WINDOW_SEC=300
CLOCK_SKEW_SEC=30
